# 后端运行期依赖
# 情绪模型（transformers/torch）为必装：新文章本地算情绪分数依赖它。
# main.py 仍保留 try/except 优雅降级，缺失时新文章情绪会退化为占位 0.5。
fastapi
uvicorn[standard]
sqlalchemy
anthropic
google-genai
python-dotenv
jieba
transformers
torch
# 半成品仓库：embedding 向量（sentence-transformers）+ 主题聚类（scikit-learn KMeans）。
# 缺失时 /vault/analyze 会逐篇在算向量处抛错回滚，导致随笔永远停在「待分析」。
sentence-transformers
scikit-learn
