# 后端运行期依赖
# 注：transformers/torch（情绪模型）为可选——main.py 已 try/except 优雅降级。
# 从 Mac 带过来的 essays.db 已存有情绪分数，画像/SOUL 功能不需要现装 torch。
# 若要让「新写文章」也能本地算情绪，再单独装：transformers torch
fastapi
uvicorn[standard]
sqlalchemy
anthropic
google-genai
python-dotenv
jieba
