# 后端运行期依赖
# 情绪模型（transformers/torch）为必装：新文章本地算情绪分数依赖它。
# main.py 仍保留 try/except 优雅降级，缺失时新文章情绪会退化为占位 0.5。
fastapi
uvicorn[standard]
sqlalchemy
anthropic
google-genai
python-dotenv
jieba
transformers
torch
