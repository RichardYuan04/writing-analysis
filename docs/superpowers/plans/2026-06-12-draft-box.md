# 草稿箱 + 清空按钮 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 写作页新增「草稿箱」（多份持久草稿，右侧第二面板）+ 编辑器「清空」按钮。

**Architecture:** 后端在单体 `backend/main.py` 内加 `Draft` 表 + 4 个 CRUD 接口（POST/GET/PUT/DELETE `/drafts`），TDD。前端新增 `DraftPanel.jsx` 面板，堆在现有 `AssistPanel` 下方；`Write.jsx` 加 `draftId` 跟踪、清空按钮、发布后删草稿。

**Tech Stack:** FastAPI + SQLAlchemy(SQLite)；pytest + TestClient（后端 TDD）；React + Vite + axios（前端构建+手动验证）。

**测试边界:** 后端走 TDD（纯 CRUD，无需 mock LLM）。前端走构建 + 手动验证（仓库无 JS 测试设施）。

---

## 文件结构
- Modify `backend/main.py`：加 `Draft` 模型、`DraftRequest`、`_draft_dict` helper、4 个接口。
- Modify `backend/tests/conftest.py`：`db` fixture 清表时一并清 `Draft`。
- Create `backend/tests/test_drafts.py`：草稿箱后端测试。
- Modify `frontend/src/api.js`：4 个草稿 API 函数。
- Create `frontend/src/components/DraftPanel.jsx`：草稿箱面板。
- Modify `frontend/src/pages/Write.jsx`：draftId、清空、发布删草稿、右列堆叠。
- Modify `frontend/src/App.css`：`.draft-panel` 与右列样式。

---

## Task 1: Draft 模型 + `POST /drafts`

**Files:**
- Modify: `backend/main.py`（`StyleProfile` 类后；assist 区块附近加接口）
- Modify: `backend/tests/conftest.py`
- Create: `backend/tests/test_drafts.py`

- [ ] **Step 1: conftest 的 db fixture 一并清 Draft 表**

在 `backend/tests/conftest.py` 的 `db` fixture 内 `_clear()` 里，`StyleProfile` 那段 try 之后加一段（保持同样的 try/except 容错，便于模型尚未存在时不报错）：

```python
        try:
            s.query(main.Draft).delete()
        except Exception:
            pass
```

（放在 `s.query(main.StyleProfile).delete()` 的 try 块之后、`s.commit()` 之前。）

- [ ] **Step 2: 写失败测试**

Create `backend/tests/test_drafts.py`：

```python
import main


def test_create_draft(client, db):
    r = client.post("/drafts", json={"title": "未完成", "content": "写到一半的内容", "date": "2026-06-12"})
    assert r.status_code == 200
    body = r.json()
    assert body["id"] > 0
    assert body["title"] == "未完成"
    assert body["content"] == "写到一半的内容"
    assert body["date"] == "2026-06-12"
    assert "updated_at" in body


def test_create_draft_rejects_empty_content(client, db):
    r = client.post("/drafts", json={"title": "x", "content": "   ", "date": "2026-06-12"})
    assert r.status_code == 400
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -v`
Expected: FAIL（`Draft` 不存在 / 接口 404）

- [ ] **Step 4: 实现模型 + helper + POST 接口**

在 `backend/main.py` 的 `StyleProfile` 类定义之后、`Base.metadata.create_all(engine)` 之前，加入：

```python
class Draft(Base):
    __tablename__ = "drafts"
    id = Column(Integer, primary_key=True)
    title = Column(String(200))
    content = Column(Text)
    date = Column(String(10))
    created_at = Column(DateTime, default=datetime.now)
    updated_at = Column(DateTime, default=datetime.now)
```

在写作工具/SOUL 接口区块之后（`@app.delete("/essays/{essay_id}")` 之前的任意位置）加入请求模型、helper 与 POST：

```python
class DraftRequest(BaseModel):
    title: str = ""
    content: str
    date: str = ""


def _draft_dict(d) -> dict:
    return {
        "id": d.id,
        "title": d.title or "",
        "content": d.content or "",
        "date": d.date or "",
        "created_at": d.created_at.isoformat() if d.created_at else None,
        "updated_at": d.updated_at.isoformat() if d.updated_at else None,
    }


@app.post("/drafts")
def create_draft(data: DraftRequest):
    if not (data.content or "").strip():
        raise HTTPException(status_code=400, detail="草稿内容不能为空")
    session = Session()
    now = datetime.now()
    d = Draft(title=data.title or "", content=data.content, date=data.date or "",
              created_at=now, updated_at=now)
    session.add(d)
    session.commit()
    result = _draft_dict(d)
    session.close()
    return result
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -v`
Expected: PASS（2 个）

- [ ] **Step 6: Commit**

```bash
git add backend/main.py backend/tests/conftest.py backend/tests/test_drafts.py
git commit -m "feat(write): Draft 模型 + 创建草稿接口"
```

---

## Task 2: `GET /drafts`（按 updated_at 倒序）

**Files:**
- Modify: `backend/main.py`（POST 接口之后）
- Modify: `backend/tests/test_drafts.py`

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_drafts.py`：

```python
def test_list_drafts_ordered_by_updated_desc(client, db):
    a = client.post("/drafts", json={"title": "A", "content": "aaa", "date": "2026-06-10"}).json()
    b = client.post("/drafts", json={"title": "B", "content": "bbb", "date": "2026-06-11"}).json()
    r = client.get("/drafts")
    assert r.status_code == 200
    ids = [d["id"] for d in r.json()]
    assert ids == [b["id"], a["id"]]  # 后建的 B 排最前


def test_list_drafts_empty(client, db):
    r = client.get("/drafts")
    assert r.status_code == 200
    assert r.json() == []
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -k list -v`
Expected: FAIL（GET 未定义 → 404，`list_drafts_empty` 也失败）

- [ ] **Step 3: 实现 GET**

在 `create_draft` 之后加入（用 `updated_at desc, id desc` 双键排序，避免同一时刻创建时顺序不确定）：

```python
@app.get("/drafts")
def list_drafts():
    session = Session()
    rows = session.query(Draft).order_by(Draft.updated_at.desc(), Draft.id.desc()).all()
    result = [_draft_dict(d) for d in rows]
    session.close()
    return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -k list -v`
Expected: PASS（2 个）

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_drafts.py
git commit -m "feat(write): 草稿列表接口（按修改时间倒序）"
```

---

## Task 3: `PUT /drafts/{id}`（更新 + 404）

**Files:**
- Modify: `backend/main.py`（GET 接口之后）
- Modify: `backend/tests/test_drafts.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_update_draft(client, db):
    d = client.post("/drafts", json={"title": "原", "content": "原内容", "date": "2026-06-12"}).json()
    r = client.put(f"/drafts/{d['id']}", json={"title": "改", "content": "改后内容", "date": "2026-06-12"})
    assert r.status_code == 200
    assert r.json()["title"] == "改"
    assert r.json()["content"] == "改后内容"
    got = client.get("/drafts").json()
    assert got[0]["content"] == "改后内容"  # 持久化生效


def test_update_draft_404(client, db):
    r = client.put("/drafts/99999", json={"title": "x", "content": "y", "date": "2026-06-12"})
    assert r.status_code == 404
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -k update -v`
Expected: FAIL（PUT 未定义）

- [ ] **Step 3: 实现 PUT**

在 `list_drafts` 之后加入：

```python
@app.put("/drafts/{draft_id}")
def update_draft(draft_id: int, data: DraftRequest):
    session = Session()
    d = session.query(Draft).filter(Draft.id == draft_id).first()
    if not d:
        session.close()
        raise HTTPException(status_code=404, detail="草稿不存在")
    d.title = data.title or ""
    d.content = data.content
    d.date = data.date or ""
    d.updated_at = datetime.now()
    session.commit()
    result = _draft_dict(d)
    session.close()
    return result
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -k update -v`
Expected: PASS（2 个）

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_drafts.py
git commit -m "feat(write): 更新草稿接口"
```

---

## Task 4: `DELETE /drafts/{id}`（删除 + 404）

**Files:**
- Modify: `backend/main.py`（PUT 接口之后）
- Modify: `backend/tests/test_drafts.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_delete_draft(client, db):
    d = client.post("/drafts", json={"title": "删", "content": "删我", "date": "2026-06-12"}).json()
    r = client.delete(f"/drafts/{d['id']}")
    assert r.status_code == 200
    assert r.json() == {"ok": True}
    assert client.get("/drafts").json() == []


def test_delete_draft_404(client, db):
    r = client.delete("/drafts/99999")
    assert r.status_code == 404
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/test_drafts.py -k delete -v`
Expected: FAIL（DELETE 未定义）

- [ ] **Step 3: 实现 DELETE**

在 `update_draft` 之后加入：

```python
@app.delete("/drafts/{draft_id}")
def delete_draft(draft_id: int):
    session = Session()
    d = session.query(Draft).filter(Draft.id == draft_id).first()
    if not d:
        session.close()
        raise HTTPException(status_code=404, detail="草稿不存在")
    session.delete(d)
    session.commit()
    session.close()
    return {"ok": True}
```

- [ ] **Step 4: 跑测试确认通过 + 全量后端测试**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/ -v`
Expected: 全部 PASS（含原有 22 + 草稿 8）

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_drafts.py
git commit -m "feat(write): 删除草稿接口"
```

---

## Task 5: 前端 API 函数

**Files:**
- Modify: `frontend/src/api.js`

- [ ] **Step 1: 新增 4 个函数**

在 `frontend/src/api.js` 末尾（`updateFragmentFeedback` 那行之后）加入：

```javascript
export const listDrafts = () => api.get('/drafts')
export const createDraft = (data) => api.post('/drafts', data)
export const updateDraft = (id, data) => api.put(`/drafts/${id}`, data)
export const deleteDraft = (id) => api.delete(`/drafts/${id}`)
```

- [ ] **Step 2: 构建验证**

Run: `cd frontend && npm run build`
Expected: 构建无报错。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/api.js
git commit -m "feat(write): 前端草稿箱 API 函数"
```

---

## Task 6: `DraftPanel` 组件

**Files:**
- Create: `frontend/src/components/DraftPanel.jsx`

- [ ] **Step 1: 创建组件**

Create `frontend/src/components/DraftPanel.jsx`：

```jsx
import { useState, useEffect } from 'react'
import { listDrafts, createDraft, updateDraft, deleteDraft } from '../api'

/**
 * 草稿箱面板（写作页右侧，AssistPanel 下方，可折叠）。
 * props:
 *  - current: {title, content, date}   当前编辑器内容
 *  - draftId: number|null              当前正在编辑的草稿 id
 *  - onSaved(draft)                    存入成功（父组件据此设 draftId）
 *  - onOpen(draft)                     点开某草稿（父组件载入编辑器）
 *  - onDraftRemoved()                  删除的正是当前草稿时通知父组件清 draftId
 *  - collapsed, onToggle               折叠状态
 */
export default function DraftPanel({ current, draftId, onSaved, onOpen, onDraftRemoved, collapsed, onToggle }) {
  const [drafts, setDrafts] = useState([])
  const [busy, setBusy] = useState(false)
  const [savedTip, setSavedTip] = useState(false)

  const refresh = () => listDrafts().then(r => setDrafts(r.data)).catch(() => {})
  useEffect(() => { refresh() }, [])

  const canSave = (current?.content || '').trim().length > 0

  const save = async () => {
    if (!canSave || busy) return
    setBusy(true)
    try {
      const payload = { title: current.title || '', content: current.content, date: current.date || '' }
      const r = draftId ? await updateDraft(draftId, payload) : await createDraft(payload)
      onSaved?.(r.data)
      setSavedTip(true); setTimeout(() => setSavedTip(false), 1600)
      refresh()
    } catch { /* ignore */ }
    setBusy(false)
  }

  const remove = async (id, e) => {
    e.stopPropagation()
    try {
      await deleteDraft(id)
      if (id === draftId) onDraftRemoved?.()
      refresh()
    } catch { /* ignore */ }
  }

  if (collapsed) {
    return (
      <aside className="draft-panel collapsed">
        <button className="dp-expand" onClick={onToggle} title="展开草稿箱">📥</button>
      </aside>
    )
  }

  return (
    <aside className="draft-panel">
      <div className="dp-head">
        <span className="dp-title">📥 草稿箱</span>
        <button className="dp-collapse" onClick={onToggle} title="收起草稿箱">▸</button>
      </div>
      <div className="dp-body">
        <div className="dp-save-row">
          <button className="dp-save" disabled={!canSave || busy} onClick={save}>
            {busy ? '存入中…' : (draftId ? '＋ 更新到草稿箱' : '＋ 存入草稿箱')}
          </button>
          {savedTip && <span className="dp-saved">已存入 ✓</span>}
        </div>
        <div className="dp-list">
          {drafts.length === 0 && <p className="dp-empty">还没有草稿。写点东西，点上面「存入草稿箱」。</p>}
          {drafts.map(d => (
            <div key={d.id} className={`dp-item ${d.id === draftId ? 'active' : ''}`} onClick={() => onOpen?.(d)}>
              <div className="dp-item-main">
                <div className="dp-item-title">{(d.title || '').trim() || '无题'}</div>
                <div className="dp-item-snip">{(d.content || '').slice(0, 40)}</div>
                <div className="dp-item-meta">{d.date || ''}{d.updated_at ? ` · ${d.updated_at.slice(0, 10)}` : ''}</div>
              </div>
              <button className="dp-del" onClick={(e) => remove(d.id, e)} title="删除草稿">🗑</button>
            </div>
          ))}
        </div>
      </div>
    </aside>
  )
}
```

- [ ] **Step 2: 构建验证**

Run: `cd frontend && npm run build`
Expected: 构建无报错。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/DraftPanel.jsx
git commit -m "feat(write): 草稿箱面板组件 DraftPanel"
```

---

## Task 7: 写作页集成（draftId / 清空 / 发布删草稿 / 右列堆叠）+ 样式

**Files:**
- Modify: `frontend/src/pages/Write.jsx`
- Modify: `frontend/src/App.css`

- [ ] **Step 1: Write.jsx —— import 与状态**

在 `frontend/src/pages/Write.jsx` 顶部 import：把第 2 行
```jsx
import { createEssay, moodReply } from '../api'
```
改为
```jsx
import { createEssay, moodReply, deleteDraft } from '../api'
```
并在 `import AssistPanel from '../components/AssistPanel'` 之后加：
```jsx
import DraftPanel from '../components/DraftPanel'
```

在状态区（`const [undoStack, setUndoStack] = useState([])` 之后）加：
```jsx
  const [draftId, setDraftId] = useState(null)             // 当前正在编辑的草稿 id
  const [draftPanelCollapsed, setDraftPanelCollapsed] = useState(false)
```

- [ ] **Step 2: Write.jsx —— 载入草稿 / 发布后删草稿 / 清空**

在 `undoLast` 函数之后加入三个处理器：

```jsx
  // 点开草稿箱里的某份 → 载入编辑器继续写
  const openDraft = (d) => {
    setTitle(d.title || '')
    setContent(d.content || '')
    setDate(d.date || today)
    setDraftId(d.id)
    setMood(null)
    setSel(null)
    setUndoStack([])
    setAutoState('saved')
    setTimeout(() => contentRef.current?.focus(), 0)
  }

  // 清空当前编辑器（带确认）；不影响草稿箱里已存的草稿
  const clearEditor = () => {
    if (!title && !content) return
    if (!window.confirm('清空当前内容？此操作不影响已存入草稿箱的草稿。')) return
    setTitle('')
    setContent('')
    setDate(today)
    setDraftId(null)
    setSel(null)
    setUndoStack([])
    setMood(null)
    setAutoState('idle')
    localStorage.removeItem(DRAFT_KEY)
  }
```

并在 `handleSave` 里，`createEssay` 成功之后、设置 mood 之前，插入「发布后把该草稿移出草稿箱」。具体：把
```jsx
      const res = await createEssay({ title, content, date })
      localStorage.removeItem(DRAFT_KEY)
```
改为
```jsx
      const res = await createEssay({ title, content, date })
      localStorage.removeItem(DRAFT_KEY)
      if (draftId) { try { await deleteDraft(draftId) } catch { /* ignore */ } ; setDraftId(null) }
```

- [ ] **Step 3: Write.jsx —— 清空按钮（编辑器底部）**

在 `write-footer` 里，把保存按钮那段
```jsx
        {mood ? (
          <span className="saved-flag">已保存 ✓</span>
        ) : (
          <button className="save-btn" onClick={handleSave} disabled={saving}>
            {saving ? '保存中…' : '保存'}
          </button>
        )}
```
改为
```jsx
        {mood ? (
          <span className="saved-flag">已保存 ✓</span>
        ) : (
          <>
            <button className="clear-btn" onClick={clearEditor} disabled={!title && !content}>清空</button>
            <button className="save-btn" onClick={handleSave} disabled={saving}>
              {saving ? '保存中…' : '保存'}
            </button>
          </>
        )}
```

- [ ] **Step 4: Write.jsx —— 右列堆叠两个面板**

把结尾处的 `<AssistPanel ... />`（整段）用一个右列容器包起来，并在其下加 `DraftPanel`。即把
```jsx
      <AssistPanel
        sel={sel}
        collapsed={panelCollapsed}
        onToggle={() => setPanelCollapsed(c => !c)}
        onApply={applyAssist}
        onUndo={undoLast}
        canUndo={undoStack.length > 0}
      />
    </div>
  )
```
改为
```jsx
      <div className="write-right">
        <AssistPanel
          sel={sel}
          collapsed={panelCollapsed}
          onToggle={() => setPanelCollapsed(c => !c)}
          onApply={applyAssist}
          onUndo={undoLast}
          canUndo={undoStack.length > 0}
        />
        <DraftPanel
          current={{ title, content, date }}
          draftId={draftId}
          onSaved={(d) => setDraftId(d.id)}
          onOpen={openDraft}
          onDraftRemoved={() => setDraftId(null)}
          collapsed={draftPanelCollapsed}
          onToggle={() => setDraftPanelCollapsed(c => !c)}
        />
      </div>
    </div>
  )
```

- [ ] **Step 5: App.css —— 右列 + 草稿箱样式**

在 `frontend/src/App.css` 末尾追加：

```css
/* 写作页右列：写作工具 + 草稿箱 竖向堆叠 */
.write-right { display: flex; flex-direction: column; gap: 14px; }

/* 草稿箱面板 */
.draft-panel {
  background: var(--bg-card); border: 1px solid var(--border);
  border-radius: 12px; padding: 14px; align-self: flex-start;
}
.draft-panel.collapsed { padding: 8px; text-align: center; }
.dp-expand, .dp-collapse {
  border: none; background: none; cursor: pointer; color: var(--accent); font-size: 15px;
}
.dp-head { display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px; }
.dp-title { font-size: 14px; font-weight: 600; color: var(--accent); }
.dp-save-row { display: flex; align-items: center; gap: 8px; margin-bottom: 10px; }
.dp-save {
  flex: 1; padding: 8px 12px; border: none; border-radius: 8px; cursor: pointer;
  background: var(--accent); color: #fff; font-size: 13px;
}
.dp-save:disabled { background: #d4c4b0; cursor: default; }
.dp-saved { font-size: 12px; color: #6a9a4a; white-space: nowrap; }
.dp-list { display: flex; flex-direction: column; gap: 6px; max-height: 320px; overflow-y: auto; }
.dp-empty { font-size: 12.5px; color: var(--text-hint); line-height: 1.6; margin: 4px 0; }
.dp-item {
  display: flex; align-items: flex-start; gap: 8px; padding: 8px 10px;
  border: 1px solid var(--border); border-radius: 8px; cursor: pointer; background: #faf8f5;
}
.dp-item.active { border-color: var(--accent); background: #f5ede0; }
.dp-item-main { flex: 1; min-width: 0; }
.dp-item-title { font-size: 13px; color: #5a4a3a; font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dp-item-snip { font-size: 12px; color: #998; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.dp-item-meta { font-size: 10.5px; color: #bbb; margin-top: 2px; }
.dp-del { border: none; background: none; cursor: pointer; font-size: 13px; opacity: 0.6; flex-shrink: 0; }
.dp-del:hover { opacity: 1; }
.clear-btn {
  padding: 7px 14px; border: 1px solid var(--border); border-radius: 8px;
  background: #fff; color: var(--text-hint); font-size: 13px; cursor: pointer; margin-right: 8px;
}
.clear-btn:disabled { opacity: 0.5; cursor: default; }
```

- [ ] **Step 6: 构建验证**

Run: `cd frontend && npm run build`
Expected: 构建无报错。

- [ ] **Step 7: Commit**

```bash
git add frontend/src/pages/Write.jsx frontend/src/App.css
git commit -m "feat(write): 草稿箱面板接入写作页 + 清空按钮 + 发布后移出草稿箱"
```

---

## Task 8: 端到端手动验证

**Files:** 无（纯验证）

- [ ] **Step 1: 重启后端 + 重建前端**

后端若在跑且带 `--reload` 会自动加载；否则重启 uvicorn。前端 `cd frontend && npm run build` 后刷新 `http://127.0.0.1:8000`（强刷）。

- [ ] **Step 2: 走草稿箱全流程**

逐项确认：
- 写作页右侧出现「📥 草稿箱」面板（在「写作工具」下方），可独立折叠。
- 写一段 → 点「＋ 存入草稿箱」→ 列表出现该草稿；按钮变「＋ 更新到草稿箱」。
- 改内容再点 → 同一条更新（不新增第二条），「上次修改」时间变新、排到最前。
- 点列表里另一条草稿 → 内容载入编辑器（标题/正文/日期）。
- 删除某条 🗑 → 从列表消失。
- 点「保存」发布 → 文章进总览，且该草稿**从草稿箱消失**。
- 「清空」按钮 → 弹确认 → 确认后编辑器清空，但草稿箱里已存草稿**仍在**。

- [ ] **Step 3: 全量后端测试**

Run: `cd backend && .\venv\Scripts\python.exe -m pytest tests/ -v`
Expected: 全部 PASS。

---

## 完成标准
- 后端 `/drafts` 四接口全部 pytest PASS。
- 草稿箱可存入（新建/更新同一份）、点开载入、删除；发布后草稿移出；清空只重置编辑器。
- 两个右侧面板独立折叠，构建无报错。
